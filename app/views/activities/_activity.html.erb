  <tr class="<%= cycle("even", "odd") %>" >
    <td><%= link_to activity.name, lesson_activity_path(@lesson, activity), :target => "_blank" %></td>
  <% if can? :manage, :all %>
    <td><%= link_to(image_tag("editar-24.png", :title => "Editar"), edit_lesson_activity_path(@lesson, activity)) %></td>
    <td><%= link_to(image_tag("apagar-24.png", :title => "Apagar"), [activity.lesson, activity], :confirm => t(:confirm), :method => :delete) %></td>
  <% end %>
  </tr>
