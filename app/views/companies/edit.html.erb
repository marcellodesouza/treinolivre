<h1 class="title-form"><%=t ".title" %></h1>

<% if current_user.role? :Gestor %>
  <%= link_to(image_tag("voltar-24.png", :title => "Voltar"), company_path(@company), :class => "back-link") %><br /><br />
<% else %>
  <%= link_to(image_tag("voltar-24.png", :title => "Voltar"), companies_path, :class => "back-link") %><br /><br />
<% end %>

<%= render 'form' %>

